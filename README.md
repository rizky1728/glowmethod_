# GlowMethod

Discord.js v14 bot with a `/method` command.

The DM menu contains one `METHOD` button. Clicking it opens the method list.

Set these environment variables:
- `DISCORD_TOKEN`
- `CLIENT_ID`

Run with:

```bash
npm install
npm start
```
